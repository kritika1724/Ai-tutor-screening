{
  "name": "ai-tutor-screener",
  "private": true,
  "version": "1.0.0",
  "description": "Deployable full-stack AI tutor screener for Cuemath",
  "scripts": {
    "postinstall": "npm --prefix ai-tutor-backend install && npm --prefix ai-tutor-frontend install",
    "dev": "./run-dev.sh",
    "build": "npm --prefix ai-tutor-frontend run build",
    "lint": "npm --prefix ai-tutor-frontend run lint",
    "check": "npm run lint && npm run build && npm --prefix ai-tutor-backend run check",
    "start": "npm --prefix ai-tutor-backend start"
  },
  "engines": {
    "node": ">=20"
  }
}
